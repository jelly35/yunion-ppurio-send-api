services:
  ppurio-server:
    restart: unless-stopped
    build:
      context: ./server
    ports:
      - "127.0.0.1:${SERVER_PORT:-7936}:5000"
    env_file:
      - .env
    volumes:
      - ./server/data:/app/data
